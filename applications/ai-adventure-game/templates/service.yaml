apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiadventure.fullname" . }}
  labels:
    app: {{ include "aiadventure.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "aiadventure.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
