loki:
  enabled: true
  persistence:
    enabled: true
    size: 5Gi
    storageClassName: local-path

promtail:
  enabled: true
  config:
    clients:
      - url: http://loki:3100/loki/api/v1/push

grafana:
  sidecar:
    datasources:
      defaultDatasourceEnabled: false

persistence:
  enabled: true
  size: 5Gi
  storageClassName: local-path
