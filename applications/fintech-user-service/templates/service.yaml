apiVersion: v1
kind: Service
metadata:
  name: {{ include "fintech.fullname" . }}
  labels:
    app: {{ include "fintech.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
  selector:
    app: {{ include "fintech.fullname" . }}
